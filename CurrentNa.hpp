#include "common.hpp"

real alpha_m(real V);
real beta_m(real V);

real alpha_h(real V);
real beta_h(real V);

real alpha_j(real V);
real beta_j(real V);

real m_inf(real V);
real h_inf(real V);
real j_inf(real V);

real CurrentNa(real V, real m, real h, real j);